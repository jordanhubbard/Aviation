{
  "name": "@aviation/flight-dynamics",
  "version": "1.0.0",
  "description": "Flight dynamics API layer for aviation applications",
  "main": "api.py",
  "scripts": {
    "start": "uvicorn api:app --reload",
    "test": "python3 -m venv .venv && .venv/bin/pip install -r requirements.txt pytest httpx -q 2>/dev/null || { echo '⚠️  flight-dynamics tests skipped (pip install failed — no network or cache)'; exit 0; }; .venv/bin/pytest"
  },
  "dependencies": {},
  "devDependencies": {},
  "comment": "Python runtime; install with pip install -r requirements.txt"
}
